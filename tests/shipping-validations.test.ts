import { describe,expect,it } from "vitest";import { carrierSchema,shipmentItemSchema,shipmentSchema,shipmentTransitionSchema } from "@/validations/shipping";
const id="00000000-0000-4000-8000-000000000001";
describe("validaciones logísticas",()=>{it("normaliza transportista y correo",()=>{const v=carrierSchema.parse({code:" tr-01 ",name:" Ficticio ",contactName:"",email:"TEST@EXAMPLE.INVALID",phone:"",trackingUrlTemplate:"https://example.invalid/{tracking}",notes:""});expect(v.code).toBe("TR-01");expect(v.email).toBe("test@example.invalid");expect(v.phone).toBeNull();});it("exige marcador seguro de seguimiento",()=>{expect(carrierSchema.safeParse({code:"TR",name:"Ficticio",contactName:"",email:"",phone:"",trackingUrlTemplate:"http://example.invalid/track",notes:""}).success).toBe(false);});it("rechaza costo y cantidad negativos",()=>{const shipment={orderId:id,carrierId:"",recipientName:"Persona ficticia",recipientPhone:"",addressLine:"Dirección ficticia",district:"",province:"",department:"",addressReference:"",shippingCost:-1,trackingNumber:"",notes:""};expect(shipmentSchema.safeParse(shipment).success).toBe(false);expect(shipmentItemSchema.safeParse({shipmentId:id,orderItemId:id,quantity:0}).success).toBe(false);});it("rechaza campos adicionales",()=>{expect(shipmentTransitionSchema.safeParse({shipmentId:id,location:"",description:"",idempotencyKey:id,status:"delivered"}).success).toBe(false);});});
