import { CustomerForm } from "@/components/sales/sales-forms";import { PageHeader,SectionCard } from "@/components/ui/operational-ui";import { requireActiveUser } from "@/lib/auth/session";
export default async function Page(){await requireActiveUser();return <section className="max-w-4xl"><PageHeader title="Nuevo cliente" description="Registra únicamente la información necesaria para la operación." backHref="/app/clientes" backLabel="Volver a clientes"/><SectionCard title="Datos del cliente"><CustomerForm/></SectionCard></section>}
