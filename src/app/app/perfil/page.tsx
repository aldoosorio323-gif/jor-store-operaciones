import { requireActiveUser } from "@/lib/auth/session";
import { Feedback,PageHeader,SectionCard,StatusBadge } from "@/components/ui/operational-ui";
export default async function ProfilePage(){const user=await requireActiveUser();return <section className="max-w-3xl"><PageHeader title="Mi perfil" description="Información de la cuenta autenticada y sus permisos actuales."/><SectionCard><dl className="divide-y divide-slate-100"><ProfileItem label="Nombre" value={user.displayName}/><ProfileItem label="Correo" value={user.email}/><ProfileItem label="Rol" value={user.roleName}/><div className="grid gap-1 py-4 sm:grid-cols-3"><dt className="text-sm font-medium text-slate-500">Estado</dt><dd className="sm:col-span-2"><StatusBadge label="Activo" tone="success"/></dd></div></dl></SectionCard><Feedback className="mt-4">El rol y el estado solo pueden cambiarse mediante una operación administrativa auditada.</Feedback></section>}
function ProfileItem({label,value}:{label:string;value:string}){return <div className="grid gap-1 py-4 sm:grid-cols-3"><dt className="text-sm font-medium text-slate-500">{label}</dt><dd className="font-medium text-slate-900 sm:col-span-2">{value}</dd></div>}
