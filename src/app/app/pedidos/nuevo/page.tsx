import { OrderForm } from "@/components/sales/sales-forms";import { PageHeader,SectionCard } from "@/components/ui/operational-ui";import { requireActiveUser } from "@/lib/auth/session";import { listCustomerOptions } from "@/services/sales";
export default async function Page(){await requireActiveUser();const customers=await listCustomerOptions();return <section className="max-w-4xl"><PageHeader title="Nuevo pedido" description="Crea el borrador; las reservas se generan únicamente al confirmar." backHref="/app/pedidos" backLabel="Volver a pedidos"/><SectionCard title="Datos del pedido"><OrderForm customers={customers}/></SectionCard></section>}
